package main

/*
	A sqlite-like database does not need a 'server' the way something like
	mySQL does. As such, the entry point to the program should be the way
	users interface with the database... so the interpreter. I suppose.
*/
